{
  "name": "kolma-app",
  "version": "1.0.0",
  "scripts": {
    "build": "prisma generate && prisma db push --accept-data-loss && next build"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "@prisma/client": "latest",
    "prisma": "latest"
  }
}
