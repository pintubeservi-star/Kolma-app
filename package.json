{
  "name": "kolma-app",
  "version": "1.0.0",
"scripts": {
  "build": "prisma db push && prisma generate && next build"
}
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "@prisma/client": "latest",
    "prisma": "latest"
  }
}
